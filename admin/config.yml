backend:
  name: github
  repo: Misbah-uddin-emran/my-blog
  branch: main
  base_url: decap-auth-ictguide.emrancsecu14701069.workers.dev

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "blog"
    label: "Blog"
    folder: "blog"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "datetime"}
      - {label: "Body", name: "body", widget: "markdown"}
